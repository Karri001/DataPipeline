name: Run Data Pipeline

on:
  schedule:
    - cron: '0 6 * * *'  # every day at 6:00 AM UTC
  workflow_dispatch:  # allows manual run anytime

permissions:
  contents: write  # allow workflow to push commits

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # ensures full history for pull/rebase

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run pipeline
      run: python pipeline.py

    # ✅ Safely sync with main branch even with local changes
    - name: Sync with latest main branch safely
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git stash push -m "temp-changes" || echo "Nothing to stash"
        git fetch origin main
        git rebase origin/main || git rebase --abort
        git stash pop || echo "No stash to pop"

    # ✅ Commit and push new outputs
    - name: Commit and push updated output
      run: |
        git add output/ pipeline_log.csv
        git commit -m "Auto-update pipeline output $(date)" || echo "No changes to commit"
        git push origin main

    # (Optional) Upload output as artifact for download
    - name: Upload output folder as artifact
      uses: actions/upload-artifact@v4
      with:
        name: pipeline_output
        path: output/
